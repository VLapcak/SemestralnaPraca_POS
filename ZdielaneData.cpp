#include "ZdielaneData.h"
#include "Manazer.h"

/*
DATA ZdielaneData::dataInit(DATA *data) {
    pthread_mutex_init(data->mutex, nullptr);
    pthread_cond_init(data->condServer, nullptr);
    pthread_cond_init(data->condKlient, nullptr);
    return {};
}*/
